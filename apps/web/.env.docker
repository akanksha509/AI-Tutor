# Docker Environment Variables for Frontend
# Used when running in Docker containers

NODE_ENV=production

# Direct API URL for Docker environment (no proxy needed)
VITE_API_URL=http://localhost:8000

# Docker specific configuration
ENVIRONMENT=docker